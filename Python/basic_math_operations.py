# nlantau, 2020-11-08


def basic_op(op, one, two):
    if op == "+": return one + two
    if op == "-": return one - two
    if op == "/": return one / two
    if op == "*": return one * two



print(basic_op("+", 4, 7))
